# Copy to .env.local (gitignored). Used only by this test app.
# Precedence: UI + localStorage > these variables > billingTestDefaults in capacitor.config.json

VITE_BILLING_TEST_PRODUCT_ID=
VITE_BILLING_TEST_PRODUCT_TYPE=INAPP
